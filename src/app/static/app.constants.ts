export const tableDataName = 'tableData';

export enum CellTypes {
  AddBtn = 'addBtn',
  Cell = 'cell',
}

export enum ModalTypes {
  New = 'new',
  Edit = 'edit'
}

export const defaultModalConfig = {
  width: '450px',
  hasBackdrop: true
}
